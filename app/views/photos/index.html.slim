= render 'breadcrumbs'

h3 = album.title

- if album.child_albums?
  = render 'albums/albums', albums: album.child_albums

- if album.photos.any?
  = render 'photos', photos: album.photos

= render 'uploader'
