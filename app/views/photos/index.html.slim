= render 'breadcrumbs'

h3 = @album.title

- if @album.child_albums.any?
  = render 'albums', albums: @album.child_albums

- if @album.photos.any?
  ul.photos data-album-id=@album.id

= render 'uploader'
